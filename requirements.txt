# Runtime prerequisite: Python >=3.10,<3.11
#
# PyTorch's default PyPI wheels include the required CUDA libraries for the
# selected platform. The explicit CUDA runtime package below also documents
# and enforces the CUDA 12 runtime used by this project.

torch>=2.2,<3.0
nvidia-cuda-runtime-cu12>=12.0,<13.0

torchrl>=0.5,<1.0
gymnasium>=0.29,<1.1
numpy>=1.26,<3.0
scipy>=1.11,<2.0

wandb>=0.16,<1.0
tensorboard>=2.16,<3.0
hydra-core>=1.3,<2.0
